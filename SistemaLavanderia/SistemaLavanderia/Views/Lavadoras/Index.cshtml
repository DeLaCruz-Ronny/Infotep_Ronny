@model IEnumerable<SistemaLavanderia.Models.Lavadora>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.DesLavadoras)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Disponible)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.DesLavadoras)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Disponible)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdLavadoras">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdLavadoras">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdLavadoras">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
